NEXT_PUBLIC_SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

ADMIN_USER=admin
ADMIN_PASS_HASH=

SESSION_SECRET=

# Optional: only needed if you want the /api/cron/cleanup endpoint to reject
# requests that don't come from Vercel Cron. Generate any random string and
# set the same value in Vercel's Cron settings as a header, or leave both
# blank to skip the check entirely.
CRON_SECRET=
